//! Build script for voice CLI (candle backend).
//!
//! No special build steps are needed for the candle backend.

fn main() {
    // Nothing to do — candle handles Metal shader compilation internally.
}
